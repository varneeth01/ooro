import type { Metadata } from "next";
import { PublicCta, PublicPage, PublicSection } from "@/components/marketing/public-page";
export const metadata: Metadata = { title: "OORO for Agencies", description: "Run offline mobility campaigns for every client from one OORO workspace." };
export default function AgenciesPage() { return <PublicPage eyebrow="For agencies" title="Run offline mobility campaigns for every client from one place." description="Create a central media operating system for client campaigns, reporting, Proof-of-Play and consolidated billing across OORO's network."><PublicSection title="A workspace for the way agencies actually operate."><div className="grid gap-4 sm:grid-cols-2">{["Multi-client campaign management", "Preferential media pricing", "Consolidated billing", "Client-level reporting", "Proof-of-Play", "Team access"].map(item => <div className="border border-neutral-200 bg-white p-5" key={item}><p className="font-medium">{item}</p><p className="mt-2 text-sm text-neutral-500">Available through an approved OORO Agency workspace.</p></div>)}</div><div className="mt-8"><PublicCta href="/signup?next=/onboarding/agency">Apply for OORO Agency</PublicCta></div></PublicSection></PublicPage>; }
