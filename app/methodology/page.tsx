import type { Metadata } from "next";
import { PublicPage, PublicSection } from "@/components/marketing/public-page";
export const metadata: Metadata = { title: "OORO methodology", description: "How OORO distinguishes verified delivery from modeled passenger opportunity estimates." };
export default function MethodologyPage() { return <PublicPage eyebrow="Methodology" title="Clear about what is verified and what is estimated." description="OORO separates device-level delivery evidence from planning benchmarks used to model passenger opportunities."><PublicSection title="Current planning benchmark"><div className="grid gap-4 sm:grid-cols-4">{[["12", "average rides / auto / day"], ["× 2", "estimated passengers / ride"], ["≈ 24", "estimated opportunities / auto-day"], ["≈ 7 min", "average ride duration"]].map(([value, label]) => <div className="border border-neutral-200 bg-white p-5" key={label}><p className="text-3xl font-medium">{value}</p><p className="mt-3 text-sm text-neutral-500">{label}</p></div>)}</div><div className="mt-8 border border-black bg-black p-6 text-white"><p className="app-eyebrow text-neutral-400">One ride-length loop</p><div className="mt-5 flex items-center gap-2 overflow-hidden text-xs"><span className="shrink-0 bg-[var(--brand-yellow)] px-3 py-3 text-black">15s creative</span>{Array.from({ length: 7 }, (_, index) => <span key={index} className="shrink-0 border border-white/30 px-3 py-3">4 slots</span>)}<span className="shrink-0 text-neutral-400">… 28 slots max</span></div><p className="mt-5 text-sm text-neutral-400">28 × 15 seconds = 420 seconds = 7 minutes. This is capacity planning, not a promise that every slot is commercially filled.</p></div></PublicSection><section className="border-y border-neutral-200 bg-white"><PublicSection title="Verified by OORO"><ul className="grid gap-3 sm:grid-cols-2">{["Campaign and creative", "Device or screen ID", "Playback timestamp and duration", "Playback success or failure", "Proof-of-Play", "Device health and location where available"].map(item => <li className="border-b border-neutral-200 py-3" key={item}>✓ {item}</li>)}</ul></PublicSection></section><PublicSection title="Estimated, not directly measured"><p>OORO estimates passenger impressions, passenger opportunities, reach and frequency from operating assumptions. Passenger impression estimates may vary based on vehicle utilisation, ride duration, passenger count, traffic, screen uptime, route behaviour and operating hours.</p></PublicSection></PublicPage>; }
