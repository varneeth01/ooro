"use client";
import { useState } from "react";
const steps = [["01", "Launch", "Create and launch a campaign with OORO."], ["02", "Move", "Your campaign travels across the mobility network."], ["03", "Reach", "Become part of the real-world movement of the city."]];
export function WorkflowSteps() { const [active, setActive] = useState(0); return <div className="grid border-t border-neutral-300 md:grid-cols-3">{steps.map(([number, title, copy], index) => <button key={number} onMouseEnter={() => setActive(index)} onFocus={() => setActive(index)} className={`group border-b border-neutral-300 py-7 text-left transition-colors md:border-b-0 md:border-r md:px-6 md:py-8 first:md:pl-0 last:md:border-r-0 ${active === index ? "text-black" : "text-neutral-500"}`}><span className="font-mono text-xs">{number}</span><span className="mt-16 block text-2xl font-medium transition-transform group-hover:translate-x-1">{title}</span><span className="mt-3 block max-w-[220px] text-sm leading-6">{copy}</span><span className={`mt-7 block h-px origin-left bg-black transition-transform duration-500 ${active === index ? "scale-x-100" : "scale-x-0"}`}/></button>)}</div>; }
