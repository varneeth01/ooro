"use client";
import Link from "next/link";
import { ArrowUpRight, Search, Sparkles } from "lucide-react";
import { useEffect, useState } from "react";
import { EmptyState, PrimaryButton, SectionCard, StatusBadge } from "./app-ui";

const prompts = ["Promote my new café in Koramangala", "Plan a ₹50K awareness campaign", "Find media around HSR Layout", "Research my audience in Bengaluru"];
export function Dashboard() { const [prompt, setPrompt] = useState(0); const [value, setValue] = useState(""); const [campaigns, setCampaigns] = useState<Array<{id:string;name:string;status:string}>>([]); useEffect(() => { const timer = window.setInterval(() => setPrompt(current => (current + 1) % prompts.length), 3500); fetch("/api/campaigns",{cache:"no-store"}).then(r=>r.ok?r.json():null).then(p=>setCampaigns(p?.data??[])).catch(()=>undefined); return () => window.clearInterval(timer); }, []); const planHref = value.trim() ? `/campaigns/new?brief=${encodeURIComponent(value.trim())}` : "/campaigns/new"; return <div className="space-y-8"><div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><div className="app-eyebrow">Your OORO workspace</div><h1 className="mt-2 text-4xl font-medium tracking-[-.05em]">Good morning.</h1><p className="mt-3 text-sm leading-6 text-neutral-500">Plan, launch and understand your offline campaigns from one place.</p></div><PrimaryButton href="/campaigns/new">Create campaign <ArrowUpRight className="ml-4" size={15}/></PrimaryButton></div><section className="overflow-hidden rounded-[12px] bg-[#111111] p-5 text-white sm:p-8"><div className="flex items-center gap-2 text-xs text-neutral-400"><Sparkles className="text-[var(--brand-yellow)]" size={15}/> OORO planning surface</div><h2 className="mt-7 text-2xl font-medium tracking-[-.03em] sm:text-3xl">What do you want to achieve?</h2><div className="mt-5 flex items-start gap-3 rounded-[9px] border border-white/15 bg-white/[.06] p-4"><Search className="mt-0.5 shrink-0 text-neutral-500" size={18}/><textarea value={value} onChange={event => setValue(event.target.value)} rows={2} placeholder={prompts[prompt]} className="min-h-[56px] w-full resize-none bg-transparent text-sm leading-6 text-white outline-none placeholder:text-neutral-500"/></div><div className="mt-4 flex flex-wrap gap-2"><Link className="rounded-[7px] bg-[var(--brand-yellow)] px-3 py-2 text-xs font-medium text-black" href={planHref}>Plan campaign</Link><Link className="rounded-[7px] border border-white/15 px-3 py-2 text-xs text-neutral-300 hover:border-white/40" href="/intelligence/research">Research market</Link><Link className="rounded-[7px] border border-white/15 px-3 py-2 text-xs text-neutral-300 hover:border-white/40" href="/discover">Find media</Link><Link className="rounded-[7px] border border-white/15 px-3 py-2 text-xs text-neutral-300 hover:border-white/40" href="/ad-lab">Test creative</Link></div></section><div className="grid gap-6 lg:grid-cols-[1.35fr_.65fr]"><SectionCard title="Active campaigns" description="Campaigns that are currently being planned or run.">{campaigns.length ? <div className="divide-y">{campaigns.slice(0,4).map(campaign => <Link href={`/campaigns/${campaign.id}`} key={campaign.id} className="flex items-center justify-between gap-4 py-4"><span className="text-sm font-medium">{campaign.name}</span><StatusBadge>{campaign.status.replaceAll("_"," ")}</StatusBadge></Link>)}</div> : <EmptyState title="No campaigns yet." description="Create your first campaign to start planning media." action={<PrimaryButton href="/campaigns/new">Create your first campaign</PrimaryButton>}/>}</SectionCard><div className="space-y-6"><SectionCard title="Recent activity"><p className="text-sm text-neutral-500">No verified workspace activity yet.</p></SectionCard><SectionCard title="Opportunities"><p className="text-sm text-neutral-500">Recommendations are unavailable until the planning provider is configured.</p></SectionCard></div></div><SectionCard title="Upcoming" description="Scheduled campaign activity will appear here."><p className="text-sm text-neutral-500">No upcoming campaigns.</p></SectionCard></div>; }
